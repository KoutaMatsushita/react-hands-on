version: '3'
services:
  server:
    build: .
    tty: true
    volumes:
      - .:/app
    ports: 
      - "8081:8081"
    command: "yarn server"
  front:
    build: .
    tty: true
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    command: "yarn dev"
